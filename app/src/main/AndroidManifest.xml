<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.brulex.bridge">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppThame">
        <activity
            android:name=".View.MainMenu"
            android:theme="@style/MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".View.NewGameMenu"
            android:theme="@style/NewGameMenu"
            android:title="@string/setting">

        </activity>
        <activity
            android:name=".View.AddPlayers"
            android:theme="@style/NewGameMenu"
            android:title="@string/players">
        </activity>
    </application>

</manifest>